# Git Commit Rule

Whenever the user writes the exact word "COMMIT" in their message, you MUST perform a git commit locally and push to the corresponding GitHub repository.

## Steps:
1. Stage all changes (`git add .`).
2. Commit with a meaningful commit message describing the recent changes (`git commit -m "..."`).
3. Push the changes to the remote branch (`git push`).
